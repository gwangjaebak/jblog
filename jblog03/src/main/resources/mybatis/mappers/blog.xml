<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="blog">
	<select id="find" resultType="blogvo">
		<![CDATA[
		   select title, logo, user_id from blog where user_id = #{id }
		]]>
	</select>

	<select id="update" resultType="blogvo">
		<![CDATA[
			update blog set title=#{title }, welcome=#{welcome }, profile=#{profile }, description=#{description }
		]]>
	</select>

</mapper>
